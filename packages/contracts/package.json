{
  "name": "@ocpp/contracts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The single source of truth for every API the CPMS, simulator and driver app speak.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "openapi": "tsx scripts/generate-openapi.ts"
  },
  "dependencies": {
    "zod": "^4.6.4"
  }
}
